/// הגדרות אופציונליות לעדכון תוכן מרחוק.
///
/// כברירת מחדל הכתובת ריקה — והאפליקציה עובדת אופליין לחלוטין ללא כל פנייה לרשת.
///
/// כדי להפעיל עדכון מרחוק (לא חובה!):
/// 1. צרו קובץ JSON בענן (למשל GitHub raw או Gist). דוגמה: wedding_config.example.json
/// 2. הדביקו כאן את הקישור הישיר ל-raw של הקובץ.
/// משאירים ריק => הפיצ'ר כבוי לגמרי.
const String kRemoteConfigUrl = '';

/// פסק זמן מקסימלי לבקשת הרשת (כדי לא לתקוע את האפליקציה אם אין חיבור טוב).
const Duration kRemoteTimeout = Duration(seconds: 6);
